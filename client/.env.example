# Base URL of the deployed backend API, including the /api prefix.
# Local dev falls back to http://localhost:5000/api when unset (see
# client/src/lib/api.ts) — only needed here for production (Vercel).
VITE_API_URL=https://your-backend.onrender.com/api
