{
  "name": "orbit",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n db,server,client -c magenta,blue,green \"npm run db --prefix server\" \"npm run dev --prefix server\" \"npm run dev --prefix client\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
